╔════════════════════════════════════════════════════════════════════════════╗
║                     SEGFAULT FIX - QUICK CHECKLIST                         ║
╚════════════════════════════════════════════════════════════════════════════╝

📋 STEP-BY-STEP FIX CHECKLIST
═══════════════════════════════════════════════════════════════════════════

☐ STEP 1: Locate the original safeQueues.h file
   Location: libAdjRaspi5Synth2/LibAPI/safeQueues.h (or similar)
   Path from CMakeLists.txt: ../../libAdjRaspi5Synth2/LibAPI

☐ STEP 2: Backup the original file
   cp safeQueues.h safeQueues.h.backup

☐ STEP 3: Replace with fixed version
   Copy content from: safeQueues_FIXED.h
   Key changes:
   ✓ Initialize msec(1) in constructor
   ✓ Add shutdown_flag(false) in constructor
   ✓ Add shutdown() method
   ✓ Call shutdown() in destructor
   ✓ Remove msec assignment from enqueue()
   ✓ Add shutdown checks in dequeue()

☐ STEP 4: Locate AlsaMidiSequencerInputClient.cpp
   Location: libAdjRaspi5Synth2/LibAPI/AlsaMidiSequencerInputClient.cpp

☐ STEP 5: Fix the destructor
   Add these 4 steps IN ORDER:
   ✓ 1. midi_in_client_thread_is_running = false
   ✓ 2. alsa_seq_client_rx_queue->shutdown()
   ✓ 3. pthread_join(midi_client_in_thread, nullptr)
   ✓ 4. delete alsa_seq_client_rx_queue

   See SEGFAULT_FIX_GUIDE.cpp for complete code

☐ STEP 6: Update midi_in_seq_client_thread() function
   Add check before enqueue:
   if (event && midi_in_client_thread_is_running) {
	   alsa_seq_client_rx_queue->enqueue(event);
   }

☐ STEP 7: Rebuild the library
   cd libAdjRaspi5Synth2
   make clean && make

☐ STEP 8: Rebuild the application
   cd AdjRaspi5qtSynthApp_2
   make clean && make

☐ STEP 9: Test the fix
   Run application and trigger MIDI input
   Close MIDI device or shutdown app
   ✓ Should NOT crash with SIGSEGV

☐ STEP 10: Verify with logging (optional)
   Add printf statements in destructor to confirm sequence
   Expected output:
	 [SHUTDOWN] Step 1: Setting running flag
	 [SHUTDOWN] Step 2: Calling queue shutdown
	 [SHUTDOWN] Step 3: Joining thread...
	 [SHUTDOWN] Step 3: Thread joined successfully
	 [SHUTDOWN] Step 4: Deleting queue
	 [SHUTDOWN] Complete


🔧 FILES CREATED IN THIS SESSION
═══════════════════════════════════════════════════════════════════════════

1. safeQueues_FIXED.h
   ✓ Complete fixed SafeQueue implementation
   ✓ All bugs corrected
   ✓ Ready to use as replacement

2. SEGFAULT_FIX_GUIDE.cpp
   ✓ Detailed code examples for AlsaMidiSequencerInputClient
   ✓ Complete destructor implementation
   ✓ Thread function updates
   ✓ Testing checklist

3. CRASH_VISUALIZATION.txt
   ✓ Visual timeline of bug vs fix
   ✓ Memory state diagrams
   ✓ Debugger evidence summary
   ✓ Proof of use-after-free

4. This checklist (FIX_CHECKLIST.txt)


🐛 ROOT CAUSE SUMMARY
═══════════════════════════════════════════════════════════════════════════

BUG: Use-After-Free in SafeQueue<snd_seq_event*>

EVIDENCE:
• 3 threads (2, 3, 4) crashed at identical address: 0x5555
• All accessing same SafeQueue: 0x555500E170A8
• Memory shows freed patterns: 0x5555, 0x5F5F5F, 0x5D7D7D
• Uninitialized msec: 8742381210945547334 (garbage)
• Threads still running during queue destruction

BUGS FOUND:
1. SafeQueue::msec uninitialized in constructor
2. SafeQueue lacks shutdown mechanism to wake threads
3. AlsaMidiSequencerInputClient deletes queue while threads running

FIX:
1. Initialize msec properly
2. Add shutdown() method with notify_all()
3. Follow proper thread shutdown: Flag → Wake → Join → Delete


⚠️  CRITICAL ORDERING
═══════════════════════════════════════════════════════════════════════════

The destructor MUST follow this exact sequence:

  1️⃣  Set flag       midi_in_client_thread_is_running = false
	   ↓
  2️⃣  Wake threads   alsa_seq_client_rx_queue->shutdown()
	   ↓
  3️⃣  Wait for exit  pthread_join(midi_client_in_thread, nullptr)
	   ↓
  4️⃣  Delete object  delete alsa_seq_client_rx_queue

  NEVER skip step 3! NEVER delete before joining!


✅ SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════════

After applying the fix:
✓ No SIGSEGV when closing MIDI devices
✓ No SIGSEGV during application shutdown
✓ Threads join successfully (no hangs)
✓ MIDI events still processed correctly
✓ No memory leaks
✓ All 3 crashing threads now exit cleanly


📞 IF STILL CRASHING
═══════════════════════════════════════════════════════════════════════════

Check these:
1. Did you apply ALL fixes (both SafeQueue AND AlsaMidiSequencerInputClient)?
2. Did you rebuild BOTH the library and the application?
3. Are there other SafeQueue instances with the same bug?
4. Are there other threads accessing the same queue?
5. Is the destructor actually being called (add logging)?
6. Check if AlsaMidiSequencerInputClient is deleted from multiple places


🎯 EXPECTED OUTCOME
═══════════════════════════════════════════════════════════════════════════

BEFORE FIX:
  [Thread 2] SIGSEGV at 0x5555 in std::__new_allocator::construct
  [Thread 3] SIGSEGV at 0x5555 in std::__new_allocator::construct
  [Thread 4] SIGSEGV at 0x5555 in std::__new_allocator::construct
  Application crashes 💥

AFTER FIX:
  [Thread 2] Exits cleanly from midi_in_seq_client_thread()
  [Thread 3] Exits cleanly from midi_in_seq_client_thread()
  [Thread 4] Exits cleanly from midi_in_seq_client_thread()
  Application shuts down gracefully ✓


═══════════════════════════════════════════════════════════════════════════
End of Checklist
═══════════════════════════════════════════════════════════════════════════
